ALTER SEQUENCE public.ads_id_seq OWNED BY ads.id;
ALTER SEQUENCE public.images_id_seq OWNED BY images.id;
ALTER SEQUENCE public.news_id_seq OWNED BY news.id;
ALTER SEQUENCE public.refreshtoken_id_seq OWNED BY refreshtoken.id;
ALTER SEQUENCE public.roles_id_seq OWNED BY roles.id;
ALTER SEQUENCE public.users_id_seq OWNED BY users.id;